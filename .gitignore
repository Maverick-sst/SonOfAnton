# Environment
.env
*.env.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
backend/.venv/

# ChromaDB persisted data
data/chroma/

# Node
frontend/node_modules/
frontend/.next/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Credentials (NEVER commit — paste GOOGLE_TOKEN_JSON env var instead)
backend/data/credentials.json
backend/data/token.json

# Eval results (regenerable)
backend/evals/results.json

# Repo-only docs (kept local; not needed for the deployed app)
AGENT.md
job.md
son_of_anton_PRD.md
son_of_anton_master_doc.md
son_of_anton_v1_fix_prd.md

# NOTE: `resume_x1.pdf` and `linkedIn_data/` ARE tracked in git.
# The Dockerfile needs them at build time to ingest the knowledge base.
# They are the candidate's own resume + LinkedIn export — public-facing data.